POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=your_db_name

# Host must be "db" when running via docker-compose (the Postgres service name).
# Use "localhost" instead if running the app outside Docker against a local Postgres.
DATABASE_URL=postgresql://your_db_user:your_db_password@db:5432/your_db_name
